#!/usr/bin/bash

if [ "${RUN_MODE}" == "train" -o -z "${RUN_MODE}" ]; then
    /opt/app-root/src/train.sh && python -m SimpleHTTPServer
elif [ "${RUN_MODE}"  == "predict" ]; then
    flask run --host=0.0.0.0 --port=8080
fi